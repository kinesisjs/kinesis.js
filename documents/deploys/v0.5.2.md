# v0.5.2 — durum

|                             |                            |
| --------------------------- | -------------------------- |
| **Versiyon (core)**         | v0.5.2 (hedef patch train) |
| **Branch**                  | `v0.5.2`                   |
| **Önceki yayın (baseline)** | [v0.5.1](./v0.5.1.md)      |
| **Durum**                   | 🚧 geliştiriliyor          |
| **Başlangıç tarihi**        | 2026-06-26                 |

## Özet

Versiyon bazlı **deploys durum-kaydı konvansiyonu** kuruldu: bundan sonra branch'ler
core sürümüyle adlandırılır ve her commit, ilgili `documents/deploys/<v-core>.md` dosyasını
son yayına göre güncel tutar. Bu commit, yeni yapının ilk kullanımıdır (kütüphane kodu
değişmedi).

## Sürüm bump'ları (per-paket)

| Paket                  | Önceki | Bu versiyon         |
| ---------------------- | ------ | ------------------- |
| @kinesisjs/core        | 0.5.1  | — (paket değişmedi) |
| @kinesisjs/openlayers  | 0.2.6  | —                   |
| @kinesisjs/leaflet     | 0.1.3  | —                   |
| @kinesisjs/angular     | 0.5.1  | —                   |
| @kinesisjs/route-aware | 0.1.3  | —                   |

> Bu yalnızca tooling/docs değişikliği; changeset yok, npm'e yayın yok.

## Feature & Modüller

- — (kütüphane özelliği değişmedi)

## Güvenlik

- — değişiklik yok

## Performans

- — değişiklik yok (paket bundle'ları etkilenmedi; `deploys/` public siteye çıkmıyor)

## Test & Kalite

- — değişiklik yok

## Dokümantasyon

- **Yeni:** `documents/deploys/` klasörü — `README.md` (konvansiyon), `_TEMPLATE.md` (şablon),
  `v0.5.1.md` (baseline snapshot), `v0.5.2.md` (bu dosya).
- **Yeni:** kök `CLAUDE.md` — branch=versiyon ve deploys kuralı; gelecekteki oturumlar buna uyar.

## Build / Release / CI

- `documents/.vitepress/config.ts` → `srcExclude: ['deploys/**']` (deploys public siteye çıkmaz).
- `.gitignore` → `!/CLAUDE.md` istisnası (kök `*.md` kuralı CLAUDE.md'yi yakalıyordu).

## Bekleyen / Bilinen boşluklar

- `docs/realtime-data-guide` PR'ı hâlâ main'e merge bekliyor (bu işten bağımsız).
- Baseline'dan devreden güvenlik hijyeni: `.github/dependabot.yml`, CI `npm audit`, `SECURITY.md`.
- Core bundle worker payload tree-shake ayrımı (9KB).

## Commit log

<!-- hash'ler commit sonrası git log'dan görülebilir; satırlar commit konularıdır -->

- chore(deploys): establish per-version status log convention + CLAUDE.md
